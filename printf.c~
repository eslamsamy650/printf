#include "main.h"

/**
 * _printf - prints anything the printf
 *           function normally prints
 *
 * Return: the number of characters printed
*/

int _printf(const char *format, ...);
{
	int sum = 0;

	if (format == NULL)
		return (-1);
}
